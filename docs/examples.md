# test-rdflib-neo4j.py

The example of how to use rdflib to build rdflib.Graph python object. The data are coming from TTL file.
TTL has RDF statements presented as specified by RDF/Turtle language. RDF statements will converted to Neo4J representation during parse() call and saved to Neo4j server as specified by Neo4jStore() object.

